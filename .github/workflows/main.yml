name: Cl

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Download ngrok
      run: sudo curl -fsSL https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-linux-amd64.zip -o ngrok.zip
    - name: Extract ngrok
      run: unzip ngrok.zip
    - name: Auth (using encrypted secret)
      run: ./ngrok authtoken ${{ secrets.NGROK_AUTH_TOKEN }}
      env:
        NGROK_AUTH_TOKEN: ${{ secrets.NGROK_AUTH_TOKEN }}
    - name: Enable SSH
      run: sudo systemctl start ssh
    - name: Allow Remote Login (**Security Risk - Consider Key-Based Authentication)  **
      run: sudo usermod -aG sudo runner  # Grant sudo access to user 'runner' (Security Risk)
    - name: Set Password (avoid plain text in code)
      run: sudo echo "P@ssw0rd!" | passwd runner
    - name: Create Tunnel (expose port 22 for SSH)
      run: ./ngrok tcp 22
